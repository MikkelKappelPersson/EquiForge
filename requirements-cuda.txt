# This file is used to install the required dependencies for the CUDA version of the project.
# It is recommended to use a virtual environment to avoid conflicts with other packages.

# PyPI packages for CUDA support
cupy-cuda12x>=12.0.0  # Python bindings for CUDA 12.x

# System dependencies (these must be installed separately via conda or NVIDIA):
# NOTE: These cannot be installed via pip and are listed here as documentation only
# cuda-nvcc>=12.8.93,<13
# cuda-nvrtc>=12.8.93,<13
# cuda-version>=12.0

# Core dependencies
-r requirements.txt
